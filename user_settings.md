# System Instructions

**Language Preference:** Always respond to the user in Japanese.
**Dotfile Management:** Files are managed using `stow` and adhere to the XDG Base Directory Specification.
**Platform Branches:** Configuration is separated to support different environments: Windows (.win), Linux (.lin), macOS (.mac), and WSL (.wsl).